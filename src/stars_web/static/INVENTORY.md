# INVENTORY — src/stars_web/static/

Static web assets served by Flask at `/static/`.
Build manifest dot-files (`._build_manifest.json`, `._cache_manifest.json`) are gitignored tool artifacts.

## Files

| File | Description |
| ---- | ----------- |
| `INVENTORY.md` | This file |

## Folders

| Folder | Description |
| ------ | ----------- |
| `css/` | Stylesheets — `star_map.css` (star map layout + scrollbars) |
| `js/` | Client-side JS — `star_map.js` (canvas rendering, planet detail panel, fleet overlays) |

## Out of Scope

- `._build_manifest.json`, `._cache_manifest.json` — build tool cache files (gitignored)

## MANIFEST

### FILES

- INVENTORY.md

### FOLDERS

- css/
- js/
